---
'@mindees/ui': patch
'@mindees/tokens': patch
'@mindees/icons': patch
---

Update npm package metadata: point `homepage` at the live docs site (https://mindees-ui.vercel.app), add `repository` (with `directory`) and `bugs` fields to `@mindees/tokens` and `@mindees/icons` so the npm listing renders proper "Repository" and "Issues" links. No code changes.
